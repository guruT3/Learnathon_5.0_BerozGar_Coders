# Security Hardening Register (HARDENING.md)

**Project:** HostelGrievance Portal  
**Scope:** Hardening and Security Assurance Assessment  
**Format:** Security Hardening Register (`HARDENING.md`)

---

## Hardening Register Table

| ID | Finding | Risk | Change | File / Location | Verification | Residual Risk |
|---|---|---|---|---|---|---|
| **HG-01** | Incomplete Object-Level Authorization (IDOR / BOLA) | Critical: Students can access, read, or modify grievances filed by other students by guessing or incrementing ticket IDs. | Implemented `assertCanViewGrievance` enforcing student ownership (`user.id === grievance.student_id`) on all read/write endpoints. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Automated unit test verifies student receiving 403 Forbidden when accessing another student's grievance. | None. Boundary strictly verified on database query and API routing layer. |
| **HG-02** | Unsafe Attachment Filename & Path Traversal | Critical: Filenames with `../` or special characters could write/read files outside the designated upload directory. | Sanitized original filenames via `originalBasename`, stored files under cryptographically randomized UUID names, and validated paths using `resolve()` bounds. | [`src/server/storage/attachments.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/storage/attachments.ts) | Automated tests verify path resolution and randomized filenames on disk. | None. File storage isolated under dedicated upload directory. |
| **HG-03** | Plaintext Session Token Storage | High: Leaked database backups would expose raw bearer tokens, allowing instant account hijacking. | Raw session tokens are hashed with SHA-256 (`hashSessionToken`) before storage and query matching in SQLite. | [`src/server/auth/session.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/auth/session.ts) | Unit test confirms DB `sessions` table contains 64-char hex hash differing from raw cookie value. | None. Token hashes cannot be reversed without brute-forcing 256 bits of entropy. |
| **HG-04** | Plaintext HTTP Transmission & Lack of TLS Enforcement | High: Unencrypted network traffic allows eavesdropping and credential/session cookie interception on Wi-Fi/LAN. | Added HTTPS redirection middleware, Strict-Transport-Security (HSTS) headers, and `REQUIRE_HTTPS` configuration. | [`src/server/app.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/app.ts), [`src/server/config.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/config.ts) | Automated test verifies 301 redirect and `Strict-Transport-Security` headers. | In local dev environments without HTTPS, proxy termination must be used for public production. |
| **HG-05** | Insecure Password Hashing (Unsalted SHA-256) | High: Rapid offline brute-forcing and precomputed rainbow-table lookups against leaked password hashes. | Upgraded password hashing to `scrypt` with unique 16-byte cryptographic salts (`scryptSync`). | [`src/server/auth/passwords.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/auth/passwords.ts) | Verified with unit tests covering `hashPassword` and constant-time `verifyPassword`. | Legacy seed accounts support fallback during transition; seed passwords should be rotated. |
| **HG-06** | Missing Security Flags on Session Cookie | High: Cookies accessible via JavaScript (XSS theft) or transmitted over unencrypted HTTP connections. | Enforced `HttpOnly: true`, `SameSite: 'Lax'`, `Path: '/'`, and dynamic `Secure: REQUIRE_HTTPS` flags on session cookies. | [`src/server/auth/session.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/auth/session.ts) | Validated `Set-Cookie` header attributes in automated login tests. | None. Cookies protected against client-side script inspection. |
| **HG-07** | Lack of Rate Limiting / Brute-Force on Login API | High: Automated credential stuffing and dictionary attacks against student and warden accounts. | Implemented rolling window rate limiting (max 5 failed attempts per 15 mins per IP/email pair) with 429 response. | [`src/server/routes/auth.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/auth.ts) | Automated test verifies 429 Too Many Requests after 5 invalid attempts. | In-memory store resets on restart; production can back with Redis. |
| **HG-08** | Stored Session Expiry Not Enforced | High: Sessions remained active indefinitely if the client maintained the token past expiration. | Validated `expires_at > nowIso()` in SQL query on every authenticated request; rejected expired tokens. | [`src/server/auth/session.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/auth/session.ts) | Automated test confirms session rejection after simulated expiry timestamp. | None. |
| **HG-09** | Logout Did Not Invalidate Database Session | High: Copied or stolen session tokens remained valid server-side even after user clicked logout. | Added explicit server-side `DELETE FROM sessions WHERE token = ?` in the `/api/logout` endpoint. | [`src/server/routes/auth.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/auth.ts) | Verified `/api/me` returns 401 Unauthorized after invoking `/api/logout`. | None. |
| **HG-10** | Overly Permissive CORS Configuration | High: Cross-origin scripts could read sensitive student grievance data if credentials were included. | Replaced wildcard CORS with strict `ALLOWED_ORIGINS` allowlist and matching request host checks. | [`src/server/app.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/app.ts) | Verified disallowed origins receive null CORS headers and blocked access. | Production requires setting `ALLOWED_ORIGINS` in environment variables. |
| **HG-11** | Stored XSS in Grievance Comments | High: Malicious script tags in comment bodies executed in the browser of the student and warden. | Rendered comment bodies as safe text in Svelte 5 and stripped/escaped HTML special characters on the server. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Tested submission of `<script>alert(1)</script>` confirming it is stored and rendered as escaped text. | None. Svelte 5 contextual auto-escaping active. |
| **HG-12** | Comments Stored Without Server-Side Sanitization | High: Unsanitized HTML in database entries could execute in external consumers or PDF exporters. | Created `sanitizeComment()` to replace `&`, `<`, `>`, `"`, and `'` with HTML entities prior to SQL insert. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Automated test confirms database row contains escaped HTML entities. | None. |
| **HG-13** | Unbounded Text Fields (Storage Exhaustion DoS) | Medium: Attackers could submit multi-megabyte payloads in title/description, exhausting disk and memory. | Enforced strict bounds at API level and database schema `CHECK` constraints (Title: 5-200, Desc: 20-5000, Comment: 1-2000). | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts), [`src/server/db/schema.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/schema.ts) | Unit tests verify HTTP 400 rejection for payloads over 200, 5000, and 2000 chars. | None. Validated at both validation and SQLite schema layers. |
| **HG-14** | Oversized Uploads Fully Loaded into Memory | Medium: Uploading huge files buffered the entire byte stream into Node.js heap memory before size checking. | Implemented streaming chunk reader with strict byte counter in `bufferFromUpload()` aborting at 2 MB. | [`src/server/storage/attachments.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/storage/attachments.ts) | Automated test confirms upload stream is aborted immediately when exceeding 2 MB. | None. |
| **HG-15** | Missing Browser Security Headers & CSP | Medium: Application lacked Clickjacking protection, MIME sniffing protection, and Content Security Policy. | Added security middleware setting CSP, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and `Referrer-Policy`. | [`src/server/app.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/app.ts) | Unit test confirms all 6 standard security headers present on HTTP responses. | None. |
| **HG-16** | Non-Atomic File and Database Operations | Medium: Failure during file write or DB insert created orphaned files on disk or broken references in SQLite. | Wrapped file write with transactional DB insert and try/catch rollback cleanup deleting disk file on failure. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Verified disk file is cleaned up if database transaction aborts. | None. |
| **HG-17** | Grievance Listing Endpoints Lacked Pagination | Medium: Loading large numbers of grievances caused unbounded memory allocation and slow database queries. | Added `page`, `limit` (max 100), `offset`, and category/status/search query parameters in `listGrievanceRows`. | [`src/server/db/queries.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/queries.ts) | Tested pagination with `page=1&limit=3` returning structured pagination metadata. | None. |
| **HG-18** | No Attachment Quota or Count Limit Per Grievance | Medium: A student could upload hundreds of attachments to a single ticket, exhausting server storage. | Enforced `MAX_ATTACHMENTS_PER_GRIEVANCE = 5` and total byte quota of 10 MB per grievance. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts), [`src/server/config.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/config.ts) | Unit test confirms 6th attachment upload is rejected with HTTP 400. | None. |
| **HG-19** | N+1 Database Query Amplification | Medium: Listing grievances executed separate queries for student user, comments, authors, and attachments for every row. | Implemented `assembleGrievancesBatch()` executing 4 bulk `WHERE IN (...)` queries with in-memory assembly in O(N). | [`src/server/db/queries.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/queries.ts) | Performance tests verify query count remains constant regardless of ticket count. | None. |
| **HG-20** | Broken Authorization on Comment Endpoints | Medium: Users could view or post comments on grievances they were not authorized to access. | Added `assertCanViewGrievance` check to both GET and POST comment routes. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Unit tests confirm student cannot comment on or read comments of other students' tickets. | None. |
| **HG-21** | Unauthorized Status Mutation by Students | Medium: Students could craft PATCH requests to change ticket status to `Resolved`. | Restricted status mutation exclusively to `warden` role; students editing tickets cannot supply `status`. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Automated test verifies student status PATCH returns 403 Forbidden. | None. |
| **HG-22** | Race Conditions in Sequential ID Generator | Medium: High concurrency could cause duplicate ID generation and SQL constraint crashes. | Enclosed `nextGrievanceId`, `nextCommentId`, and `nextAttachmentId` inside atomic SQLite transaction blocks. | [`src/server/routes/grievances.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/grievances.ts) | Verified atomic execution and sequential ID generation under concurrent requests. | None. |
| **HG-23** | Content-Disposition Header Injection | Medium: Filenames with newline or quote characters could corrupt HTTP headers and cause reflected XSS. | Sanitized header filenames by stripping ASCII control characters `[\x00-\x1F\x7F"\\]` and using UTF-8 RFC-5987 encoding. | [`src/server/routes/attachments.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/attachments.ts) | Verified attachment response headers with special characters in original filename. | None. |
| **HG-24** | Missing Anti-CSRF Protections | Medium: Malicious external websites could forge authenticated state-changing POST/PATCH requests. | Implemented Origin and Referer validation plus `Sec-Fetch-Site: cross-site` blocking on all mutation methods. | [`src/server/app.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/app.ts) | Verified cross-site origin and sec-fetch-site requests are rejected with 403 Forbidden. | None. |
| **HG-25** | MIME Spoofing & Dangerous File Uploads | Medium: Attackers could upload executable scripts disguised as images by spoofing `Content-Type`. | Implemented `validateMagicBytes()` inspecting binary file signatures for JPEG, PNG, GIF, and WebP formats. | [`src/server/storage/attachments.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/storage/attachments.ts) | Tested rejection of `.txt` files disguised as `image/png`. | None. |
| **HG-26** | Verbose Error Messages Leaking Stack Traces | Medium: Database syntax errors or filesystem paths were returned to clients in error responses. | Normalized error responses via `HttpError` returning clean JSON codes (`bad_request`, `unauthorized`, `not_found`). | [`src/server/http/errors.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/http/errors.ts) | Verified 404 and 500 error bodies contain zero stack trace or internal path leakage. | None. Detailed error logging kept server-side only. |
| **HG-27** | Client-Side Navigation State Trusted for Auth | Medium: Manipulating `localStorage` session cache allowed bypassing frontend UI role views. | Enforced authoritative server-side role validation on every API route; UI cache verified asynchronously with `/api/me`. | [`src/lib/services/api.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/lib/services/api.ts), [`src/server/routes/auth.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/auth.ts) | Verified API rejects spoofed client credentials regardless of `localStorage` state. | None. |
| **HG-28** | Unlimited Active Sessions Per Account | Medium: Attackers with compromised credentials could create unlimited session records, bloating database. | Enforced `MAX_ACTIVE_SESSIONS_PER_USER = 5`, automatically revoking oldest surplus tokens on each login. | [`src/server/auth/session.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/auth/session.ts) | Automated test verifies logging in 7 times leaves only the 5 most recent sessions. | None. |
| **HG-29** | Attachment Downloads Buffered Full File in Memory | Medium: `readFileSync` loaded entire attachment bytes into memory on download, risking OOM under load. | Replaced with `streamStoredFile()` using Node.js `Readable.toWeb()` to stream file chunks directly. | [`src/server/storage/attachments.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/storage/attachments.ts) | Verified streaming attachment responses with accurate `Content-Length` and headers. | None. |
| **HG-30** | Missing Dependency Lockfile | Medium: Build reproducibility risked dependency tampering or supply chain updates introducing vulnerabilities. | Generated and committed deterministic `package-lock.json` with cryptographic SHA-512 integrity hashes. | [`package-lock.json`](file:///d:/final%20project/GIET-Learnathon-5.0-main/package-lock.json) | Verified deterministic clean build via `npm ci`. | Regular dependency vulnerability audits (`npm audit`) should be maintained. |
| **HG-31** | Missing Audit Trail for Grievance Actions | Low: Changes to grievance status and ticket edits were not logged, preventing accountability. | Created `grievance_audit_logs` table recording actor ID, action type, old value, new value, and ISO timestamp. | [`src/server/db/schema.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/schema.ts), [`src/server/db/queries.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/queries.ts) | Verified audit log creation on grievance creation and warden status update. | None. |
| **HG-32** | Weak Operating System File Permissions | Low: SQLite database and uploaded attachments created with default system permissions on shared hosting. | Applied `0o600` (rw-------) for files and `0o700` (rwx------) for directories via `chmodSync`. | [`src/server/db/connection.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/connection.ts), [`src/server/config.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/config.ts) | Verified permission enforcement in server startup and DB initialization routines. | POSIX file modes apply on Linux/macOS; Windows relies on NTFS ACLs. |
| **HG-33** | Hardcoded Plaintext Default Credentials | Low: Seed script stored default student/warden credentials that could remain active in production. | Added production warnings, password hashing in seeder, and environment separation guidelines. | [`src/server/db/seed.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/seed.ts), [`src/server/config.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/config.ts) | Verified database initialization and documentation. | Seed credentials intended strictly for development labs; must not be deployed to public servers. |
| **HG-34** | Mock Service Bypassed Authorization | Low: Client mock layer allowed updating any grievance status or spoofing comment authors. | Added role and ownership validation checks into `src/lib/services/mock.ts`. | [`src/lib/services/mock.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/lib/services/mock.ts) | Tested mock service rejection when student attempts to update status. | Production builds use HTTP API client exclusively. |
| **HG-35** | Destructive Database Reset Lacked Safeguard | Low: Accidental invocation of reset command in production would permanently destroy live application data. | Added `ALLOW_DESTRUCTIVE_RESET=true` environment safeguard throwing errors in production mode. | [`src/server/db/reset.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/reset.ts) | Verified `resetDatabase()` throws Error when `NODE_ENV=production` without override flag. | None. |
| **HG-36** | Health Probe Exposed Internal Information | Low: Unrestricted probe endpoints assisted automated discovery and service fingerprinting. | Configured minimal `/api/health` probe returning `{ status: "pass" }` with `Cache-Control: no-store`. | [`src/server/app.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/app.ts) | Verified probe response and headers. | None. |
| **HG-37** | `X-Forwarded-For` IP Spoofing Rate Limit Bypass | High: Attackers could spoof IP headers to bypass login brute-force rate limiting. | Added `TRUST_PROXY` validation, sanitized IP regex matching, and fallback to direct peer address. | [`src/server/routes/auth.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/auth.ts), [`src/server/config.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/config.ts) | Verified rate limiter evaluates trusted IP and rejects spoofed headers. | `TRUST_PROXY=true` must only be set when behind a verified reverse proxy. |
| **HG-38** | Rate Limit Memory Exhaustion Flood | Medium: Flooding login with randomized fake emails created unbounded Map entries, crashing Node.js. | Added bounded capacity (`MAX_RATE_LIMIT_ENTRIES = 5000`) with auto-LRU pruning of expired and oldest entries. | [`src/server/routes/auth.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/auth.ts) | Verified memory capacity enforcement under high-volume simulated failed attempts. | None. Memory strictly bounded under 5000 entries. |
| **HG-39** | Password Timing Attack (Email Enumeration) | Medium: Different computation times between existing and non-existing accounts revealed valid user emails. | Implemented `dummyVerifyPassword()` executing constant-time `scrypt` hashing when user is not found. | [`src/server/auth/passwords.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/auth/passwords.ts), [`src/server/routes/auth.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/routes/auth.ts) | Verified timing equalization across valid and invalid email login requests. | None. Constant computational complexity maintained. |
| **HG-40** | Host Header Poisoning & Open Redirect | Medium: Manipulating the `Host` header could redirect users to attacker-controlled phishing domains. | Created `isAllowedHost()` validating `Host` against strict `ALLOWED_HOSTS` allowlist. | [`src/server/app.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/app.ts), [`src/server/config.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/config.ts) | Unit test confirms request with untrusted `Host` header returns 400 Bad Request. | Production domains must be included in `ALLOWED_HOSTS`. |
| **HG-41** | Image Decompression Pixel Bomb (Client-Side DoS) | Medium: Uploading a 500 KB image with 50,000×50,000 resolution could exhaust browser memory and freeze client. | Built binary header parser `validateImageDimensions()` enforcing max 4K (4096×4096px) resolution. | [`src/server/storage/attachments.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/storage/attachments.ts) | Unit test verifies PNG with 5000×5000 dimensions is rejected with HTTP 400. | None. Validated for PNG, JPEG, GIF, and WebP formats. |
| **HG-42** | SQLite Database Lock Contention on Wildcard Search | Medium: Unbounded wildcard search queries (`%term%`) could lock SQLite table and cause `SQLITE_BUSY` errors. | Configured `db.pragma('busy_timeout = 5000')` and truncated search terms to `MAX_SEARCH_QUERY_LENGTH = 100`. | [`src/server/db/connection.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/connection.ts), [`src/server/db/queries.ts`](file:///d:/final%20project/GIET-Learnathon-5.0-main/src/server/db/queries.ts) | Verified search query truncation and concurrent database query resilience. | Full-text search index (FTS5) recommended if database scales to 100k+ records. |
